# Railway 루트 기준 nixpacks 빌드 설정
# fpdf2 한글 폰트 지원을 위해 NanumGothic 설치 필수

[phases.setup]
nixPkgs = ["python311", "nanum-fonts"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
